const smallestStringWithSwaps = function (s, pairs) {
  // const unionFind = new UnionFind()
  // pairs.forEach((pair) => {
  //   unionFind.merge(pair[0], pair[1])
  // })
  // const disjoinSets = unionFind.getDisjointSets()
  // const setsWithChar = disjoinSets.map((set) => {
  //   return set.map((idx) => s[idx])
  // })
};

module.exports = { smallestStringWithSwaps };
